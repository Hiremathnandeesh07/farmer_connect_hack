<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Buy Form</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-weight: 300;
        }

        body {
            font-family: "Source Sans Pro", sans-serif;
            color: white;
            font-weight: 300;
            background: #ffffff;
            /* Change background color to blue */
            width: 100%;
            height: 100%;
        }

        .container {
            margin: 10% auto;
            padding: 33px 0;
            text-align: center;
        }

        .container h1 {
            font-size: 40px;
            font-weight: 200;
        }

        input {
            border: 1px solid rgba(255, 255, 255, 0.4);
            /* Change border color to white */
            background-color: rgba(38, 169, 221, 0.2);
            /* Change background color to white with opacity */
            width: 250px;
            border-radius: 3px;
            font-family: "Source Sans Pro", sans-serif;
            padding: 13px 15px;
            margin: 0 auto 10px auto;
            display: block;
            text-align: center;
            font-size: 18px;
            color: black;
            /* Change text color to blue */
            font-weight: 300;
        }

        input::placeholder {
            color: black;
            /* Change placeholder text color to blue */
        }

        button {
            appearance: none;
            outline: 0;
            background-color: #007bff;
            /* Change background color to blue */
            border: 0;
            padding: 10px 15px;
            color: white;
            border-radius: 3px;
            width: 250px;
            font-size: 18px;
        }

        button:hover {
            background-color: #0056b3;
            /* Darker blue on hover */
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Buy Form</h1>
        <form action="/final_buy" method="POST">
            <input name="address" placeholder="Address" />
            <input name="city" placeholder="City" />
            <input name="state" placeholder="State" />
            <input name="pincode" placeholder="Pincode" />
            <input name="quantity" placeholder="Quantity (in kg)" type="text" />
            <button type="submit">Submit</button>
        </form>
    </div>
</body>

</html>